name: 🔁 Update LICENSE Year

on:
  schedule:
    - cron: '0 0 1 1 *' # every Jan 1st at midnight UTC
  workflow_dispatch:

jobs:
  update-license:
    runs-on: ubuntu-latest

    steps:
      - name: 🧾 Checkout Repository
        uses: actions/checkout@v3

      - name: 🕒 Get current year
        id: date
        run: echo "year=$(date +'%Y')" >> "$GITHUB_OUTPUT"

      - name: 🔍 Replace year in LICENSE file
        run: |
          OLD_YEAR=$(grep -oP 'Copyright \(c\) \K\d+' LICENSE)
          NEW_YEAR=${{ steps.date.outputs.year }}
          
          if [ "$OLD_YEAR" != "$NEW_YEAR" ]; then
            echo "Updating LICENSE year from $OLD_YEAR to $NEW_YEAR"
            sed -i "s/$OLD_YEAR/$NEW_YEAR/g" LICENSE
          else
            echo "✅ LICENSE already up-to-date"
          fi

      - name: ✅ Commit changes (if any)
        run: |
          git config user.name "rushhiii"
          git config user.email "rushiofficial1205@gmail.com"

          if [[ $(git status --porcelain) ]]; then
            git add LICENSE
            git commit -m "🔁 Update LICENSE year to ${{ steps.date.outputs.year }}"
            git push https://x-access-token:${{ secrets.GH_PAT }}@github.com/${{ github.repository }} HEAD:main
          else
            echo "✅ No changes to commit"
          fi
